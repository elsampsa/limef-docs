# Hello World Example

This is a simple example showing how limef works.

```python
from limef import SomeClass
```

First, we create an instance:

```python
obj = SomeClass("test")
```

Then we call a method on it:

```python
result = obj.do_something()
print(f"Result: {result}")
```
