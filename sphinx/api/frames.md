# Frames

Frames encapsulate all data and signals that flow through a pipeline.
The base class is {cpp:class}`Limef::frame::Frame`; subclasses are identified by
{cpp:enum}`Limef::frame::FrameClass`.

```{eval-rst}
.. doxygennamespace:: Limef::frame
   :members:
```
